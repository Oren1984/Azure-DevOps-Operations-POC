apiVersion: v1
kind: Service
metadata:
  name: adops-poc
  namespace: adops-poc
spec:
  type: ClusterIP
  selector:
    app: adops-poc
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
